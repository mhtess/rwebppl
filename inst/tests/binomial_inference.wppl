var n = 5
var data = [1,4,2,4,1,2,0]

var model = function(){
	var theta = beta(1,1)

	var score = sum(map(function(d){
		return binomialERP.score([theta,n], d);
		}, data
	))

	factor(score)

	return {'theta':theta}
}

var posterior = MCMC(model,{
	samples: 10000,
	verbose: true
})

posterior